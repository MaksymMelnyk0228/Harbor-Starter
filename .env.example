# Harbor local defaults
# PORT and CLIENT_ORIGIN are set automatically:
#   npm run dev  → API :3001, Vite :5173
#   npm start    → everything on :3000
DATABASE_URL="file:./dev.db"
JWT_SECRET=harbor-local-secret
JWT_EXPIRES_IN=7d
HOST=0.0.0.0
